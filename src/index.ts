// StatCard — the proven (erevna+katalogos) metric tile.
// DataTable + StatGrid are NOT here yet: they currently exist only in kefi-web (n=1).
// Per "extract on the 2nd use, not the first", they'll move in when a 2nd consumer appears.
export { StatCard } from './StatCard/StatCard';
export type { StatCardProps } from './StatCard/StatCard';
